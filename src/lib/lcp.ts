import { siteConfig } from "@/data/photography";

/** LCP hero image — keep in sync with HeroShell. */
export const HERO_IMAGE_SRC =
  "https://images.unsplash.com/photo-1470071459604-3b5ec3a7fe05?w=1200&q=75";

export const HERO_IMAGE_ALT = `Cinematic landscape photography by ${siteConfig.name} — portfolio hero`;
