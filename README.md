# photography

Cinematic photography portfolio (Next.js).

## SEO & deployment

Set your production URL before deploying:

```bash
NEXT_PUBLIC_SITE_URL=https://your-domain.com
```

Copy `.env.example` to `.env.local` for local overrides. This drives canonical URLs, `sitemap.xml`, and `robots.txt`.
